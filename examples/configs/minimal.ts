/**
 * Minimal Configuration
 *
 * Bare minimum setup - just specify a preset.
 * Perfect for personal projects or getting started.
 */
import { defineConfig } from 'commit-it'

export default defineConfig({
	preset: 'conventional',
})
